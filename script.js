const header=document.querySelector('header'),menu=document.querySelector('.menu'),nav=document.querySelector('nav'),bar=document.querySelector('.progress');menu.onclick=()=>nav.classList.toggle('open');document.querySelectorAll('nav a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));addEventListener('scroll',()=>{header.classList.toggle('scrolled',scrollY>30);bar.style.width=(scrollY/(document.documentElement.scrollHeight-innerHeight)*100)+'%'});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');if(e.target.classList.contains('stat'))count(e.target.querySelector('b'));io.unobserve(e.target)}}),{threshold:.15});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));function count(el){let n=+el.dataset.count,s=el.dataset.suffix||'',start=performance.now(),dur=1500;function tick(t){let p=Math.min((t-start)/dur,1),v=Math.floor(n*(1-Math.pow(1-p,3)));el.textContent=v.toLocaleString()+s;if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick)}
